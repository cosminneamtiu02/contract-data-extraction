"""OCR engine integration tests and engine-specific unit tests (Phase 2).

Tests here exercise the concrete ``DoclingOcrEngine`` and, once Task 2.5 lands,
PDF-level smoke-tests against real (synthetic) contract PDFs.  Unit tests for
the Protocol / value-object layer (``OcrResult``, ``OcrEngine``) live in
``tests/unit/``.
"""
